import React from 'react';

function FormComponente() {
    return (
        <div>
            <input type="text" name="componente" />
            <button>Duplicate</button>
            <button>Delete</button>
        </div>
    );
}

export default FormComponente;
